<h1>Burgers!</h1>

<h2>Burgers that are not eaten!</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
    <li>{{burger_name}}</li>
      {{!-- {{> Burgers/Burger-block sleep=true}} --}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that are eaten!</h2>

<ul>
  {{#each burgers}}
  {{!-- <li>{{burger_name}}</li> --}}
    {{#if devoured}}
    <li> {{burger_name}}</li>
    {{/if}} 
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="devaured">Devaured?</label><br>
    <input type="radio" name="Devaured" value="1" checked>Devaured<br>
    <input type="radio" name="Devaured" value="0"> Not Eaten
  </div>

  <button type="submit">Add Burger</button>
</form>